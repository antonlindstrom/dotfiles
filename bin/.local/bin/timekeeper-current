#!/bin/bash

if [ -f /usr/bin/timekeeper ]; then
	/usr/bin/timekeeper ls --format="" --stats --stats-format '{{if .IsActive}}{{if .IsOverLimit}}{{else}}{{end}} {{.DurationString}}{{end}}' --today
else
	echo ""
fi
