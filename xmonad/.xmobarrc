Config { font = "xft:Inconsolata:size=10:antialias=true,FontAwesome:pixelsize=10,Pomodoro:pixelsize=10",
       , additionalFonts = []
       , bgColor = "black"
       , fgColor = "grey"
       , alpha = 200
       , position = Top
       , textOffset = -1
       , iconOffset = -1
       , lowerOnStart = True
       , pickBroadest = False
       , persistent = False
       , hideOnStart = False
       , iconRoot = "."
       , allDesktops = True
       , overrideRedirect = True
       , commands = [ Run Date "%a %b %_d %Y %H:%M:%S" "date" 10
                    -- cpu activity monitor
                    , Run MultiCpu [ "--template", "CPU: <total>%" ] 20
                    -- memory usage monitor
                    , Run Memory [ "--template", "Mem: <usedratio>%" ] 20
                    -- battery status
                    , Run Com "battery-status" [ "--icon" ] "battery-icon" 120
                    , Run Com "battery-status" [] "battery" 60
                    -- bitcoint amount
                    , Run Com "btc-amount" [] "btc" 600
                    -- bitcoint trend since yesterday
                    , Run Com "btc-trend" [] "btctrend" 600
                    -- stock status
                    , Run Com "stock-indicator" [] "stocks" 600
                    -- timekeeper status
                    , Run Com "timekeeper-current" [] "timekeeper" 20
                    , Run StdinReader
                    ]
       , sepChar = "%"
       , alignSep = "}{"
       , template = "  %StdinReader% }\
                    \{ <fc=#657b83>%timekeeper% %stocks%  %btctrend% %btc%  %multicpu%  %memory% %battery-icon% Battery: %battery% |</fc>  %date% "
       }
